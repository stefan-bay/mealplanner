[package]
name = "mealplanner"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
axum = "0.8"
handlebars = "6"
serde = { version = "1", features = ["derive"] }
tower-http = { version = "0.6", features = ["fs"] }
